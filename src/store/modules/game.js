const state = {
  state: 'welcome',
  connected: false,
  cells: new Array(9).fill(null),
  turn: 'X'
}

const getters = {}

const actions = {}

const mutations = {}

export default {
  namespaced: true,
  state,
  getters,
  actions,
  mutations
}
